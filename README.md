# setup

- Installare Docker Desktop
- Eseguire comando `docker compose up` per eseguire il container 
    - utilizzare opzione `-d` per lanciare in background
- L'applicazione sarà disponibile su `http://localhost:8001`