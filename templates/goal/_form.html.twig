{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

    {{ form_row(form.name) }}

    {{ form_row(form.quantity) }}

    {{ form_row(form.goalQuantity) }}

    {{ form_row(form.expiration) }}

    {{ form_row(form.save, {'label': 'Save'}) }}

{{ form_end(form) }}
